# IndigoTest

Тестовое задание: имитаторы бирж + агрегатор котировок.

Разработать имитацию системы сбора, обработки и хранения ценовых данных с нескольких бирж в реальном времени.

## Что внутри

- `IndigoTest.Simulators.First` — первый WebSocket-имитатор.
- `IndigoTest.Simulators.Second` — второй WebSocket-имитатор.
- `IndigoTest.Simulators.Third` — третий WebSocket-имитатор.
- `IndigoTest.Simulators.Common` — общая логика симуляторов: генерация тиков, сбои, статус, WebSocket-цикл.
- `IndigoTest.Aggregators.Web` — точка входа агрегатора и простая страница для проверки.
- `IndigoTest.Aggregators.Operations` — основная логика агрегатора: слушатели, парсеры, транспорт, дедупликация, обработка.
- `IndigoTest.Aggregators.Persistence` — PostgreSQL, контекст, миграция, запись пачками.
- `IndigoTest.Aggregator.Domain` — доменная сущность тика.
- `IndigoTest.Aggregators.Operations.Tests` — тесты на нормализацию, дедупликацию, батчинг, ретраи и переподключение.

## Важно. Тестирование

Сначала тестировал вручную через отключение контейнеров симуляторов, перезапуском с нужными параметрами. Но решил упростить процесс вам и мне лично - попросил ИИ сгенерировать простую страницу с возможностью управления имитаторами и аггрегатором. 
Я вижу, что код, написанный ИИ-шкой максимально фиговый - Некорректные эндпоинты, генерация HTML из строки в коде :)

### Исправил бы

1. Статусы: Использование нормальных MediatR и CQRS для получения статусов работы через 
Состав эндпоинтов по REST был бы:
```
GET  /api/simulator/status
PUT  /api/simulator/faults - конечно, немного странно отключать имитаторы через REST

GET  /api/aggregator/status
GET  /api/aggregator/dashboard
```
2.  Слой .Web. Во-первых - избавился бы от супер-класса Service, вынес всю бизнес-логику подключений в ответственный за него класс. Добавил бы нормальную реализацию подписок. В целом переход на SignalR
3. Так же переименовал бы весь проект, переименовал доменную сущность. Так как MarketTick не отражает никакой нормальной информации. Пример:  CurrencyQuotation. 
4. Нормализовал таблицу Aggregator_MarketTick. Вынес бы Source в отдельную таблицу. Чисто теоретически можно рассмотреть подход TPH, но очень рискованно в поддержке. Только в случае, когда модель данных в коде сильно будет зависеть от формата данных в зависимости от какого-то атрибута. Допустим Source, или типа котировки 
5. Пересмотрел бы работу с циклами и асинхронностью.
 Я в целом не так много работал со стримингом данных, поэтому где-то делал по интуиции. 

## Как запустить

Из папки `src/IndigoTest`:

```powershell
docker compose up --build -d
```

- агрегатор: `http://localhost:8080`
- первый симулятор: `http://localhost:8081`
- второй симулятор: `http://localhost:8082`
- третий симулятор: `http://localhost:8083`
- PostgreSQL: `localhost:5432`

## Настройки

Сделал работу симулятора и агрегатора управляемым через передачу параметров окружения в environment контейнера aggregator

Параметры:

- `Simulator__TickIntervalMs`
- `Simulator__TicksPerIteration`
- `Simulator__Symbols__0`
- `Aggregator__Transport__Capacity`
- `Aggregator__Deduplication__Window`
- `Aggregator__Write__BatchSize`
- `Aggregator__Write__RetryDelay`
- `Aggregator__Reconnect__InitialDelay`
- `Aggregator__Reconnect__MaxDelay`
- `Aggregator__Liveness__IdleTimeout`



## Нагрузка

Симуляторы настроены примерно на 1000 тиков в секунду суммарно. Сервер работал на протяжении 3-х часов, загрузил более 10 млн строк. По профилированию в ide график памяти ровный

## Форматы бирж

Форматы по ТЗ разные

Первый источник

```json
{
  "symbol": "BTCUSDT",
  "price": 67000.15,
  "volume": 1.25,
  "timestamp": "2026-07-05T10:00:00+00:00"
}
```

Второй источник

```json
{
  "s": "ETHUSDT",
  "p": "3450.42",
  "v": "2.75",
  "ts": 1783245600000,
  "kind": "ticker"
}
```

Третий источник

```json
{
  "instrument": "SOLUSDT",
  "trade": {
    "last": 152.30,
    "qty": 4.125
  },
  "meta": {
    "time": "05.07.2026 10:00:00.123",
    "source": "third"
  }
}
```

Внутри агрегатора все приводится к одному виду:

```text
source, symbol, price, volume, timestamp, receivedAt
```

## Дедупликация

Дубликатом считается тик с одинаковыми:

- `Source`
- `Symbol`
- `Price`
- `Volume`
- `Timestamp`

Окно дедупликации: `1 минута`
 симуляторы при дублировании отправляют тот же самый тик второй раз. Для такого тестового этого ключа достаточно. `ReceivedAt` в ключ не входит, потому что повтор может прийти позже

## Переподключение

Агрегатор держит отдельное WebSocket-подключение к каждому источнику

Если один источник упал или закрыл сокет:

- остальные источники продолжают работать
- проблемный источник переподключается отдельно
- задержка переподключения растет по экспоненте
- максимум задержки сейчас `30 секунд`

Еще есть idle-timeout. Если сокет формально живой, но данные перестали приходить, агрегатор не ждет бесконечно. Он считает соединение зависшим и переподключается

## Запись в БД

Реализован батчинг данных. Размер батча - управляется через параметры приложения
Посчитал, что для потоковых данных применение трекинга будет сильно излишним
Корректнее реализация - через бинарную вставку, которую поддерживают многие СУБД. Применил Binary COPY от PostgreSQL

сценарий, где БД временно не отвечает:
- текущий батч не выбрасывается
- ретрай с задержкой
- транспортный канал ограничен по размеру
- когда канал заполняется, чтение из источников начинает ждать
- не добавленные данные - дописываются в БД в порядке очереди.

## Graceful shutdown

При остановке агрегатора источники завершаются, транспорт закрывается, а обработчик дописывает то, что уже успело попасть в очередь

## Мониторинг
На странице есть отображение всех счетчиков, которые высчитываются через SourceLivenessMonitor;
Есть счетчики:

- `receivedTicks`
- `processedTicks`
- `duplicateTicks`
- `writtenTicks`
- `droppedTicks`
- `pendingTicks`

`pendingTicks` показывает заполненность внутренней очереди. Если БД не отвечает или запись не успевает, этот счетчик начнет расти.


## Тесты

Не хватило времени. реализовал бы через NUnit или через XUnit. Большого опыта нет, но проблем в них не вижу.
